#include <cub.h>
#include <stdio.h>

int main(int ac, char **av) {
  if (ac != 2) {
    /* TODO:  write the error func */
    // ft_error(ac);
    perror("Error: argummment not valid");
  }
  /* TODO:  ft_pacre */
  // ft_parce(av);
  return (0);
}
